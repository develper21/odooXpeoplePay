"use client";

import { useForm } from "react-hook-form";
import { z } from "zod";
import type { Employee } from "@/types/domain";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

const schema = z.object({ employeeNumber: z.string().min(3, "Employee code is required"), firstName: z.string().min(2, "First name is required"), lastName: z.string().min(2, "Last name is required"), email: z.string().email("Enter a valid email"), phone: z.string().optional(), department: z.string().min(1, "Department is required"), position: z.string().min(2, "Job position is required"), managerId: z.string().optional(), scheduleId: z.string().min(1, "Working schedule is required"), employeeType: z.enum(["FULL_TIME", "PART_TIME", "CONTRACT"]), status: z.enum(["ACTIVE", "INACTIVE", "ON_LEAVE"]), joinedOn: z.string().min(1, "Join date is required") });
type FormValues = z.infer<typeof schema>;

export function EmployeeForm({ initialValues, managers, schedules, onSubmit, submitting, submitLabel = "Save Employee", onCancel }: { initialValues?: Partial<Employee>; managers: Employee[]; schedules: { id: string; name: string }[]; onSubmit: (values: FormValues) => Promise<void>; submitting?: boolean; submitLabel?: string; onCancel: () => void }) { const { register, handleSubmit, setError, formState: { errors } } = useForm<FormValues>({ defaultValues: { employeeNumber: initialValues?.employeeNumber ?? "", firstName: initialValues?.firstName ?? "", lastName: initialValues?.lastName ?? "", email: initialValues?.email ?? "", phone: initialValues?.phone ?? "", department: initialValues?.department ?? "Engineering", position: initialValues?.position ?? "", managerId: initialValues?.managerId ?? "", scheduleId: initialValues?.scheduleId ?? schedules[0]?.id ?? "", employeeType: initialValues?.employeeType ?? "FULL_TIME", status: initialValues?.status ?? "ACTIVE", joinedOn: initialValues?.joinedOn ?? new Date().toISOString().slice(0, 10) } }); const submit = async (values: FormValues) => { const result = schema.safeParse(values); if (!result.success) { result.error.issues.forEach((issue) => setError(issue.path[0] as keyof FormValues, { message: issue.message })); return; } await onSubmit(result.data); }; const field = (name: keyof FormValues, label: string, type = "text") => <label className="block text-sm font-medium">{label}<Input className="mt-2" type={type} {...register(name)} />{errors[name] && <span className="mt-1 block text-xs text-danger">{errors[name]?.message}</span>}</label>; return <form onSubmit={handleSubmit(submit)} className="space-y-6"><section><h2 className="border-b pb-3 text-sm font-semibold">Personal / Identity</h2><div className="mt-4 grid gap-4 md:grid-cols-2">{field("firstName", "First name *")}{field("lastName", "Last name *")}{field("employeeNumber", "Employee code *")}{field("email", "Email *", "email")}{field("phone", "Phone")}</div></section><section><h2 className="border-b pb-3 text-sm font-semibold">Work Information</h2><div className="mt-4 grid gap-4 md:grid-cols-2">{field("department", "Department *")}{field("position", "Job position *")}{<label className="block text-sm font-medium">Manager<select className="mt-2 h-10 w-full rounded-md border bg-surface-raised px-3 text-sm" {...register("managerId")}><option value="">No manager</option>{managers.map((manager) => <option key={manager.id} value={manager.id}>{manager.firstName} {manager.lastName}</option>)}</select></label>}{<label className="block text-sm font-medium">Working schedule *<select className="mt-2 h-10 w-full rounded-md border bg-surface-raised px-3 text-sm" {...register("scheduleId")}><option value="">Select schedule</option>{schedules.map((schedule) => <option key={schedule.id} value={schedule.id}>{schedule.name}</option>)}</select>{errors.scheduleId && <span className="mt-1 block text-xs text-danger">{errors.scheduleId.message}</span>}</label>}{<label className="block text-sm font-medium">Employee type<select className="mt-2 h-10 w-full rounded-md border bg-surface-raised px-3 text-sm" {...register("employeeType")}><option value="FULL_TIME">Full time</option><option value="PART_TIME">Part time</option><option value="CONTRACT">Contract</option></select></label>}{<label className="block text-sm font-medium">Status<select className="mt-2 h-10 w-full rounded-md border bg-surface-raised px-3 text-sm" {...register("status")}><option value="ACTIVE">Active</option><option value="ON_LEAVE">On leave</option><option value="INACTIVE">Inactive</option></select></label>}{field("joinedOn", "Joined on *", "date")}</div></section><div className="flex justify-end gap-3 border-t pt-5"><Button type="button" variant="secondary" onClick={onCancel}>Cancel</Button><Button type="submit" disabled={submitting}>{submitting ? "Saving..." : submitLabel}</Button></div></form>; }
